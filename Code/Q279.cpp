#include <stdio.h>
#include <math.h>

using namespace std;

class Solution {
public:
    int numSquares(int n) {
        int bound = floor(sqrt(n));
        for (int i = bound; i > 0; i--) {
            
        }
    }
};