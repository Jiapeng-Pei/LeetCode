#include <stdio.h>
#include <iostream>

using namespace::std;

class Solution {
public:
    int divide(int dividend, int divisor) {
        if (diviend == 0) return 0;
        bool isPostive = ((dividend < 0) == (divisor < 0));
        int res = 0;
        
    }
};

int main() {
    Solution s;
    cout << s.divide(2147483647, 1) << endl;
}
